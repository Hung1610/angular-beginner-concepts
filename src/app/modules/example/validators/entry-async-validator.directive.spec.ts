import { EntryAsyncValidatorDirective } from './entry-async-validator.directive';

describe('EntryAsyncValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EntryAsyncValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
